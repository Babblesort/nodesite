extends layout

block content
	a(href="/newuser") Add New User

	h3.
		User List
	ul
		each user, i in userlist
			li
				a(href="mailto:#{user.email}")= user.username
				&mdash;
				span= user.email

